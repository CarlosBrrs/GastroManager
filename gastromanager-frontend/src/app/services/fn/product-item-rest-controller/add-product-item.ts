/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import {HttpClient, HttpContext, HttpResponse} from '@angular/common/http';
import {Observable} from 'rxjs';
import {filter, map} from 'rxjs/operators';
import {StrictHttpResponse} from '../../strict-http-response';
import {RequestBuilder} from '../../request-builder';

import {ApiGenericResponseUuid} from '../../models/api-generic-response-uuid';
import {ProductItemRequestDto} from '../../models/product-item-request-dto';

export interface AddProductItem$Params {
  body: ProductItemRequestDto
}

export function addProductItem(http: HttpClient, rootUrl: string, params: AddProductItem$Params, context?: HttpContext): Observable<StrictHttpResponse<ApiGenericResponseUuid>> {
  const rb = new RequestBuilder(rootUrl, addProductItem.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({responseType: 'blob', accept: '*/*', context})
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ApiGenericResponseUuid>;
    })
  );
}

addProductItem.PATH = '/api/v1/product-items';
